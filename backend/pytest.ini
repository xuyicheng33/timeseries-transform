[pytest]
# 测试目录
testpaths = tests

# 异步模式
asyncio_mode = auto

# 输出配置
addopts = -v --tb=short --strict-markers

# 标记定义
markers =
    unit: 单元测试
    integration: 集成测试
    slow: 耗时测试

# 忽略警告
filterwarnings =
    ignore::DeprecationWarning
    ignore::PendingDeprecationWarning

