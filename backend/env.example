# 时序数据预测平台 - 环境变量配置示例
# 复制此文件为 .env 并修改配置

# ============ 应用配置 ============
APP_NAME="Time Series Analysis Platform"
APP_VERSION="1.0.0"
DEBUG=true

# ============ 服务器配置 ============
HOST=0.0.0.0
PORT=8000

# ============ 数据库配置 ============
# SQLite (开发环境)
DATABASE_URL=sqlite+aiosqlite:///./timeseries.db

# PostgreSQL (生产环境示例)
# DATABASE_URL=postgresql+asyncpg://user:password@localhost:5432/timeseries

# ============ JWT 认证配置 ============
# 重要：生产环境必须设置此值！
# 可以使用以下命令生成：python -c "import secrets; print(secrets.token_urlsafe(32))"
JWT_SECRET_KEY=your-super-secret-key-change-in-production
JWT_ALGORITHM=HS256
JWT_ACCESS_TOKEN_EXPIRE_MINUTES=30
JWT_REFRESH_TOKEN_EXPIRE_DAYS=7

# ============ 数据隔离配置 ============
# false = 团队共享模式（所有用户可见所有数据）
# true = 用户隔离模式（用户只能看到自己的数据和公开数据）
ENABLE_DATA_ISOLATION=false

# ============ 文件上传配置 ============
# 最大上传文件大小（字节），默认 100MB
MAX_UPLOAD_SIZE=104857600

# ============ 缓存配置 ============
# 缓存最大保留天数
CACHE_MAX_AGE_DAYS=7
# 缓存最大大小（MB）
CACHE_MAX_SIZE_MB=1024

# ============ CORS 配置 ============
# 允许的前端域名，多个用逗号分隔
CORS_ORIGINS=["http://localhost:5173", "http://127.0.0.1:5173"]
