# 时序预测平台 - Docker 环境变量配置
# 复制此文件为 .env 并修改配置
# 使用方法：docker-compose up -d

# ============ 前端端口 ============
# 前端服务暴露的端口（默认 80）
FRONTEND_PORT=80

# ============ JWT 密钥（必填！）============
# 重要：必须设置此值，否则 docker-compose 启动会失败！
# 生成方法：python -c "import secrets; print(secrets.token_urlsafe(32))"
JWT_SECRET_KEY=

# ============ 数据隔离 ============
# false = 团队共享模式（所有用户可见所有数据）
# true = 用户隔离模式（用户只能看到自己的数据和公开数据）
ENABLE_DATA_ISOLATION=false
